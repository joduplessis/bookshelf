var React = require('react');
var ReactDOM = require('react-dom');
var App = require('./components/App.jsx');

// Render the primary app component
ReactDOM.render(<App/>, document.getElementById('app'));
